{
  "annotations": {"list": []},
  "editable": true,
  "panels": [
    {
      "type": "timeseries",
      "title": "Request Duration (p95)",
      "targets": [{
        "expr": "histogram_quantile(0.95, sum(rate(demo_request_duration_seconds_bucket[1m])) by (le))"
      }],
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0}
    },
    {
      "type": "timeseries",
      "title": "Request Rate",
      "targets": [{
        "expr": "sum(rate(demo_requests_total[1m]))"
      }],
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0}
    }
  ],
  "schemaVersion": 38,
  "style": "dark",
  "time": {"from": "now-15m", "to": "now"},
  "title": "Flask App - Overview",
  "version": 1
}
